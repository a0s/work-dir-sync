# generated by install.sh (contains absolute paths)
com.local.work-dir-sync.plist

# rclone bookkeeping written next to the filters file
*.md5

# logs / scratch
*.log

# just in case: real configs never belong here (they live in ~/.config/work-dir-sync)
config.sh
